name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  GO_VERSION: "1.25.8"

jobs:
  # ============================================
  # Build - Cross-compile openctem-admin CLI
  # ============================================
  build:
    name: Build (${{ matrix.os }}/${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: amd64
            runner: ubuntu-latest
          - os: linux
            arch: arm64
            runner: ubuntu-latest
          - os: darwin
            arch: amd64
            runner: ubuntu-latest
          - os: darwin
            arch: arm64
            runner: ubuntu-latest
          - os: windows
            arch: amd64
            runner: ubuntu-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build openctem-admin
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
        run: |
          BINARY_NAME="openctem-admin"
          if [ "${{ matrix.os }}" = "windows" ]; then
            BINARY_NAME="openctem-admin.exe"
          fi

          go build \
            -ldflags="-s -w -X main.Version=${{ steps.version.outputs.VERSION }}" \
            -o "bin/${BINARY_NAME}" \
            ./cmd/openctem-admin

      - name: Package binary
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          ARCHIVE_NAME="openctem-admin-${VERSION}-${{ matrix.os }}-${{ matrix.arch }}"

          cd bin
          if [ "${{ matrix.os }}" = "windows" ]; then
            zip "../${ARCHIVE_NAME}.zip" openctem-admin.exe
          else
            tar czf "../${ARCHIVE_NAME}.tar.gz" openctem-admin
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: openctem-admin-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            *.tar.gz
            *.zip
          if-no-files-found: error
          retention-days: 1

  # ============================================
  # Release - Create GitHub Release
  # ============================================
  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: /tmp/artifacts
          pattern: openctem-admin-*
          merge-multiple: true

      - name: Generate checksums
        working-directory: /tmp/artifacts
        run: |
          sha256sum *.tar.gz *.zip > checksums-sha256.txt
          cat checksums-sha256.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          append_body: true
          body: |
            ## Docker Images

            Images are published to **GitHub Container Registry** (multi-arch: amd64 + arm64):

            ```bash
            # API
            docker pull ghcr.io/openctemio/api:${{ steps.version.outputs.VERSION }}

            # Database Migrations
            docker pull ghcr.io/openctemio/migrations:${{ steps.version.outputs.VERSION }}

            # Database Seed
            docker pull ghcr.io/openctemio/seed:${{ steps.version.outputs.VERSION }}

            # Admin CLI
            docker pull ghcr.io/openctemio/admin-cli:${{ steps.version.outputs.VERSION }}
            ```

            > Images are also mirrored to Docker Hub (`openctemio/*`) if configured.

            ## Deployment

            ```bash
            # Update version in your .env.versions file
            API_VERSION=${{ steps.version.outputs.VERSION }}
            MIGRATIONS_VERSION=${{ steps.version.outputs.VERSION }}

            # Pull and restart
            docker compose pull
            docker compose up -d
            ```
          files: |
            /tmp/artifacts/*.tar.gz
            /tmp/artifacts/*.zip
            /tmp/artifacts/checksums-sha256.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
