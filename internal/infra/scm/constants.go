package scm

// Constants for SCM providers
const (
	// Connection messages
	connectionSuccessful = "Connection successful"

	// GitLab URLs
	defaultGitLabURL = "https://gitlab.com"

	// HTTP Client
	defaultUserAgent = "SCM-Client/1.0"
)
